{
  "name": "big-scroll",
  "private": true,
  "scripts": {
    "test": "node --test web-tests/*.test.mjs",
    "test:browser": "playwright test",
    "test:baseline": "node web-tests/compare-upstream.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
